# Monster 缺失分类记录

> 生成日期：2026-04-16
> 源路径：`\\172.16.8.156\art-data-intern\FF7EC\model\monster\`
> 输出路径：`\\172.16.8.156\art-data-intern\FF7EC\output\monster\`

## 概述

112 个 monster 源分类中，有 **4 个未生成 GLB/PNG 输出**，原因是这些分类下 **不包含 `model/` 子目录**，没有可处理的 3D 模型文件（OBJ/FBX）。

## 缺失列表

| 分类 | 目录内容 | 说明 |
|------|----------|------|
| `039` | `battle/`, `motion/` | 仅有战斗 AI 数据（JSON）和动作数据，无模型 |
| `051` | `battle/` | 仅有战斗 AI 数据（JSON），无模型 |
| `066` | `battle/` | 仅有 1 个战斗配置 JSON，无模型 |
| `067` | `battle/` | 仅有战斗 AI / 死亡动画数据（JSON），无模型 |

## 详细说明

这 4 个分类的共同特征：
- 源目录下**没有 `model/` 子目录**
- 只包含 `battle/`（战斗 AI 配置）和/或 `motion/`（动作数据）
- 文件均为 JSON 格式，属于游戏逻辑配置，不包含 3D 网格或贴图资源
- pipeline 扫描 `{category}/model/` 时直接跳过，未报错

## 统计

- 源分类总数：112
- 已输出：108（含 77 个多材质模型）
- 缺失：4（无模型数据，非处理失败）
